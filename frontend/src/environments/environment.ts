export const environment = {
  production: true,
  // Backend base URL. Override per-environment; reviewers can point this at their local backend
  // without touching component code.
  apiBaseUrl: 'http://localhost:8080',
};
